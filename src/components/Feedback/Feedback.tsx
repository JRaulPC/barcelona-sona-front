import { ToastContainer } from "react-toastify";

const Feedback = (): React.ReactElement => {
  return (
    <div className="error">
      <ToastContainer />
    </div>
  );
};

export default Feedback;
